# Next actions

1. **Restore/reconcile estate CI**
   - Verify whether `.github/workflows/ci.yml` should match the pre-boundary-sync estate workflow.
   - Keep `.github/workflows/boundary-sync.yml` as a focused additive check.

2. **Exact algebraic screening**
   - Implement optional exact primitive/irreducible/Pisot filters.
   - Keep core tests stdlib-only.

3. **Manuscript v12.2 insertion**
   - Add boundary synchronization section to the current manuscript.
   - State BSL as a theorem.
   - State synchronizing-end criterion as partial only.
   - State nonsynchronizing zero-return trap as boundary normal form.
   - State Newborn Synchronizing Boundary as the next conjectural target.

4. **Archive discipline**
   - Historical claims may remain in `archive/`, but current manuscripts and canonical docs must pass the source audit.
