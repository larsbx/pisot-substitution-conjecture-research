# Audit — 2026-09-20: the overlap-productivity route against the C1–C4 reduction chain

**Scope.** `main` at `c2acce8` (through PR #133), the 497 commits since the
09-10 audit, the manuscript `PSC_balanced_pair_state_2026-09-13.tex`, its
102-round referee record, the regenerated proof ledger, and the eleven
post-09-14 attack notes on Open Problem 5.35. Method: every theorem on the new
sufficiency route was re-derived by hand from the manuscript text; the
referee record, the attack chronology, and the status surfaces were read in
full by delegated readers and their findings were spot-checked. Mojo, TLA+
and Lean were not runnable in this container; CI on `c2acce8` is green on all
three workflows.

**Headline.** The route to PDS was rebuilt between 09-11 and 09-14. The old
boxed conditional `G1 ∧ C1 ⟹ PDS` survives as Theorem 3.1, but the shortest
route is now Theorem 5.38: *productivity of every overlap reachable from one
swap seed ⟹ PDS*, with no finiteness hypothesis. Every proof on that route
that is the repository's own checks under re-derivation. Its one open premise, however, is not a
reduction of the conjecture but a restatement of it: it contains two-sided
strong coincidence (Arnoux–Ito, open for `d ≥ 3` since 2001) as its aligned
case, and for unimodular `σ` the manuscript itself notes a converse in
Barge–Kwapisz, so there Open Problem 5.35 is *equivalent* to PDS. The C1–C4
chain I audited on 09-10 is now a side route: its graph-level and
Parikh-level rungs transferred to the overlap graph and are load-bearing
there; its word-level rungs (orientation, signed defects, all degree sieves,
mid-area) have no overlap analogue and are off the critical path.

---

## A. What happened since 09-10

| Item | State |
|---|---|
| 09-10 audit actions 1–2 (extend `Ledger.tla`, sync prose) | done: `tla/Ledger.tla` is now generated from `ledger.json` with 61 nodes including `C2 ← C3Local ← C4` as one-way `Requires` edges; README, ledgers and proof ladder rewritten 09-11/09-14 |
| 09-10 action 3 (settle whether PDS ⟹ G1) | resolved conservatively: the manuscript does **not** assert it (Open Problem 4.24, seed legality and seed choice unverified in ABBLS Thm 5.3); the referee separately forced withdrawal of a stray "G1 follows from PDS" sentence |
| 09-10 action 5 (uniform-in-`|C|` target via prefix-difference returns and recognizability) | pursued: return-gap lemma, legal ancestry towers, derived recognizability (manuscript §5.9). All sit in the G1-conditional programme and did not close anything |
| New: bounded discrepancy G1b-1 (Thm 4.4) | proved; short and correct (§B) |
| New: finite seed-patch overlap graph (Thm 4.22) | proved; correct |
| New: coincidence density (Thm 5.32), three forms (Lemma 5.36), main theorem without finiteness (Thm 5.38) | proved modulo one import (§B) |
| New: BD endpoint eliminator (type G impossible on three letters) | proved; correct |
| Manuscript | 1,111 lines, seven-status vocabulary, 102 automated referee rounds with 169 findings, all accepted; ~28 mathematical, the rest a PDF-integrity tool |
| Python suite | 38 local failures, all in `test_check_manuscript_source.py`, all from `pypdf` failing to import in this container (`PanicException`); CI passes the same tests. Environment, not code |

---

## B. The new sufficiency route, proof by proof

`PIP` = primitive, irreducible characteristic polynomial, Pisot dilation.
Every step was re-derived from the manuscript text.

| Step | Statement | Verdict |
|---|---|---|
| Lemma 2.4 | tile lengths `ℓ_a` are `Q`-independent | correct (cyclic vector under `Mᵀ`) |
| Thm 4.4 (G1b-1) | `Disc(T) ≤ 4(d+1)C_σ` for every reachable state | correct. Prefix decomposition + summable contraction on `E_s` bounds `‖P_sπ(prefix)‖`; the zero-length lemma `‖x‖ ≤ (d+1)‖P_sx‖` for `⟨1,x⟩=0` controls the Perron component. Uses only primitivity and `ρ(M|E_s)<1`; no unimodularity, no UD |
| Thm 4.22 | overlap types occurring in swap pairs have integer offset vectors with `‖w‖_∞ ≤ B_σ`; `O_σ` finite | correct; child relation on types is well defined |
| Thm 5.32 (i)–(v) | common fraction monotone; productive iff `δ>0` iff some overlap productive; all reachable overlaps productive ⟺ `δ(s)=1`; `δ(s)=1` ⟹ all reachable states productive; under finiteness both ⟺ termination with coincidence | correct. The forward direction of (iii) is a uniform-depth contraction of non-common length; the converse uses that a common tile inside an inflated nonproductive intersection would be a coincidence descendant |
| Cor 5.33, 5.34 | `ρ(N_S)=β`, `N_S V_S = V_S Mᵀ`, `rank V_S = d`, `spec M ⊆ spec N_S` for closed non-coincidence `S` | correct; overlap transport of the Parikh intertwiner. Post-referee restriction to non-coincidence sets is necessary and sufficient for all later uses |
| Lemma 5.36 | reachable overlaps productive ⟺ `δ(s)=1` ⟺ good set `G(s)` dense | correct after the referee fix: `G_m` are not nested, but `G_m ∖ G_{m'}` is finite (finitely many subdivision points per level), which is all (c)⟹(a) needs. Note (b) also gives `G(s)` of **full measure**, stronger than dense |
| Imported 5.37 (BSW Thm 3.1) | periodic patch `Q̄`, `Q̄ − ℓ_a` densely eventually coincident ⟹ PDS | **not independently verifiable here.** See B.1 |
| Thm 5.38 | one-seed overlap productivity ⟹ PDS | correct given 5.37 |
| Prop 5.39 | `(i,j,0)` productive ⟺ `{i,j}` eventually coincident; right-aligned ⟺ reversal; hence 5.35 ⟹ two-sided strong coincidence | correct |
| Prop 5.40, Cor 5.41 | boundary hit at level `m` ⟺ `M^m w ∈ P_m(i) − P_m(j)`; under strong coincidence productivity ⟺ hitting | correct (post-fix: left endpoints only) |
| Prop 5.42 | contracting lower bound on hitting depth | correct (triangle inequality in each contracting embedding) |
| Prop 5.43 | nonproductive vertex ⟹ closed irreducible nonproductive `S`, Perron-critical, full rank | correct (sink extraction, every non-coincidence has a child) |
| Prop 5.44 | aligned pairs avoid eventual coincidence and are closed under `σ_±`; dichotomy aligned / strict zipper | correct |
| Lemma 5.45 | ordered cycle equation `(I − M^r)w_0 = Σ M^{r−1−k} d_k`, purely periodic offset expansion, no vanishing tail in the zipper case | correct; `I − M^r` invertible since no conjugate of `β` is a root of unity |
| Thm 5.16 (BD eliminator) | closed nonproductive set on three letters: no zero return has an eventually coincident right pair; neither endpoint map is a 3-cycle; hub-letter normal form | correct. Uses only the existence clause of Barge–Diamond Thm 1; the first-block orbit argument is clean; reversal handles the suffix map |

### B.1 The import that carries the whole G1-free claim

Theorem 5.38 rests entirely on Imported Theorem 5.37. The referee record
(102 rounds) never examined its hypotheses; the response says so
(`response-to-reviewers` l.168). The manuscript's own hypothesis check
(l.780–782) asserts: primitivity, aperiodicity and FLC hold; `ℓ_a, ℓ_b` are
`Q`-independent; the patch need not be legal. Three things to verify against
Barge–Štimac–Williams §3 by a human reader, in order of risk:

1. that Theorem 3.1 accepts a periodic patch `Q̄` that is **not** a legal
   patch of `Ω_σ` (the swap word `ab` may be an illegal factor);
2. that "densely eventually coincident" means coincidence on a dense set of
   `x`, not on a set of positive or full measure. If it is the latter, the
   proof still goes through: Lemma 5.36(b) gives `G(s)` of full measure. The
   manuscript should state the full-measure form so the import is robust to
   either reading;
3. that the one-dimensional specialization is exactly the argument by which
   their Theorem 3.2 is derived from 3.1, as claimed.

Until (1) is checked, Theorem 5.38 should be read as "conditional on the
import as stated".

### B.2 Steps I did not re-derive

The G1-conditional programme's newer theorems (legal ancestry towers,
derived recognizability, relative hierarchy-offset state, Thm 5.19 uniform
return, Prop 5.20 wedge dichotomy) were read but not re-derived line by
line; they are off the headline route. The 09-10 audit's verdicts on the
C1–C4 rungs stand; the referee's mathematical findings against them (F1, F3,
F7: concentration reinstated as open, the `|det M|>1, |C|=3 ⟹ r ≥ 5` clause
withdrawn, Wielandt equality case written out) were all corrections in the
direction of *fewer* claims, and I concur with each.

---

## C. The overlap route against the reduction chain

This is the question the audit was asked. Notation: `C1` = SCC Producer,
`C2 ⟸ C3-local ⟸ C4` the boundary chain, `OP` = Open Problem 5.35 (all
overlaps of one swap seed productive), `OP_all` = every vertex of `O_σ`
productive.

### C.1 Logical relationship (all proved in the manuscript)

```text
OP_all  ⟹  every reachable BPA state productive  ⟹  C1        (Thm 5.32 iv)
OP      ⟹  PDS                                               (Thm 5.38)
under G1:   OP_all  ⟺  C1                                    (Thm 5.32 iii,v + Prop 4.23)
under G1:   C4 ⟹ C3-local ⟹ C2 ⟹ C1                          (Prop 5.7 chain; one-way)
G1 ∧ C1 ⟹ PDS                                                (Thm 3.1)
```

So `OP` is a *strengthening* of `C1` that (a) does not need G1 and (b) lives
on a graph that is finite unconditionally. The C4 chain is a sufficiency
route for `C1` under G1 only. It cannot feed `OP` directly: a nonproductive
overlap may lie inside a productive BPA state (Thm 5.32(ii) says a state is
productive iff *some* overlap of it is), so state-level productivity does
not imply overlap-level productivity without finiteness.

### C.2 Which rungs transferred

| 09-10 rung | Overlap analogue | Load-bearing now? |
|---|---|---|
| sink-SCC reduction | Prop 5.43 closed irreducible obstruction | yes |
| Parikh intertwiner, `ρ(N_C)=β`, rank `d` | Cor 5.33–5.34 mass balance, full rank | yes |
| boundary synchronization, C3 locality | Prop 5.39/5.44(i): aligned overlaps ⟺ eventual coincidence; `Sync_±` criterion | yes |
| endpoint cores, signature bound | Prop 5.44(ii): `σ_±`-cycles of non-eventually-coincident pairs in `A_±(S)` | yes |
| BD type-G eliminator, hub-star | formulated on BPA states; transfers verbatim to `A_+(S)` | yes |
| orientation cocycle, even/odd spectra | none: an overlap type `(i,j,t)` carries no word | no |
| signed defect intertwiners, `W_det`, degree-4 floor, mod-3 sieve, parity | none | no |
| mid-area identity, mean-area lift, Cramer certificate | none | no |

Ten of the eighteen results audited on 09-10 are therefore parked in
"parallel programme B" (finite-BPA carriers under G1). That is the correct
classification and the status surfaces say so.

### C.3 What the new gate actually is

Three facts, all proved in the manuscript, fix the difficulty of `OP`:

1. `OP ⟹` two-sided strong coincidence (Prop 5.39(iii)). Strong coincidence
   is the Arnoux–Ito condition, a theorem for `d=2` (Barge–Diamond 2002) and
   open for `d ≥ 3`. So `OP` is at least as hard as a named open problem that
   predates this program by twenty-five years.
2. Under strong coincidence, `OP` reduces to the hitting problem
   `∃m: M^m w ∈ P_m(i) − P_m(j)` for every vertex (Cor 5.41(ii)). The
   manuscript identifies this (l.868) as "the geometric form of the
   coincidence problem studied in [IR, BK, MT]", i.e. super-coincidence /
   geometric coincidence. That is the second half of the classical
   decomposition PDS ⟺ strong coincidence + super-coincidence.
3. For unimodular `σ`, PDS ⟹ productivity of every vertex of `O_σ` can be
   read off Barge–Kwapisz Cor 9.4 / Prop 17.2 (manuscript l.793; not recorded
   as imported). With Thm 5.38 this makes `OP_all ⟺ PDS` in the unimodular
   case.

Consequently the program has, by its own route, arrived at the standard
equivalent form of the Pisot conjecture. That is a correctness signal for the
route and a novelty warning for the claim. "One open premise" is literally
true and rhetorically misleading: the premise is the conjecture, minus a
finiteness hypothesis that the literature's overlap algorithm (Akiyama–Lee,
via the Meyer property) also never needed. The genuine gains are narrower and
should be stated as such:

- Thm 4.22 gives finiteness of the *seed-patch* (possibly illegal) overlap
  graph from bounded discrepancy alone, with an explicit bound;
- Prop 5.43–5.44 and Lemma 5.45 give a normal form (Perron-critical, full
  rank, aligned-or-zipper, purely periodic cycle offsets) that is sharper
  than "residual component of full growth";
- Prop 5.42 is the first place the program uses Pisot contraction at the
  offset level rather than through eigenvalue moduli, which the 09-10 audit
  asked for. It is, however, a *lower* bound on the hitting depth (early hits
  are impossible), which is the wrong direction for forcing a hit.

### C.4 State of the attack on the gate (from the eleven post-09-14 notes)

The strict-zipper branch was refined in five layers, each closing a
sub-obligation named by the previous note and each accompanied by a
countermodel showing the sub-obligation was insufficient:

| Date | Layer | Countermodel |
|---|---|---|
| 09-14 | normal form, dichotomy (PR #88) | Akiyama–Lee Thm 4.1: `ρ(N_S)=β` is the hard case, not a contradiction |
| 09-15 | ordered affine pump `w' = Mw + q − p`, cycle identity (PR #91) | det-2 substitution `0→1, 1→021, 2→001`: six-edge zero-shift-free cycle inside a productive graph |
| 09-15 | context equality | same substitution: equal affine state, unequal one-step symbolic context |
| 09-16 | radius-`m` collar (PR #101) | the pump lifts to collared cycles at every tested radius; `1→213, 2→3, 3→131` has a collision no radius ≤ 12 separates |
| 09-17 | seed-growth accounting (PR #115) | accounting proved; the rigidity step (full-growth closed nonproductive realized component) untouched since |
| 09-18 | pivot to the aligned branch: coincidence level ≤ coaccessible core size − 1 (PRs #122–#127); prefix-translation normal form (PR #133) | unimodular family `0→1, 1→2, 2→0 2^n` shows no bounded-height premise is available; both notes state they cannot close #84 alone |

`docs/exploration-openings-2026-09-18.md` records "no progress on Open
Problem 5.35". This is the same saturation pattern the 09-10 audit described
for the C4 sieves, now on the overlap side: each layer is a correct necessary
condition calibrated against one golden negative, and no layer comes with a
completeness statement.

Two asymmetries worth recording: the dichotomy in Prop 5.44(iii) is
left-aligned only (right-aligned vertices can sit inside the "zipper" branch;
5.44(i) still constrains them but the zipper description does not); and the
uniform coincidence-level bound of PR #127 is conditional on nonemptiness of
the pair language, which its own executable cannot certify.

---

## D. Assessment

1. **The route is sound and the record is honest.** Every theorem on the
   09-14 route checks, the manuscript's status vocabulary is used correctly,
   the referee's 28 mathematical findings were all accepted and all reduced
   claims, and the ledger now encodes the chain as one-way dependencies. The
   09-10 record-drift finding has been fully remediated.

2. **The single unexamined dependency is the BSW import.** No reviewer,
   automated or human, has checked the hypotheses of Imported Theorem 5.37
   against the source. Everything that distinguishes the new route from the
   old one (the disappearance of G1) rests on that one citation. B.1 lists the
   three points to check; the full-measure strengthening is free and should
   be made.

3. **The open premise is the conjecture.** `OP` contains strong coincidence,
   is equivalent to PDS for unimodular `σ`, and under strong coincidence is
   the super-coincidence hitting problem. The program has reconstructed the
   standard picture, which is reassuring about the mathematics and should
   temper the "one open premise" framing on every status surface.

4. **Pisot geometry still enters only through the spectrum, plus one lower
   bound.** The zipper branch's constraints (Perron-criticality, full rank,
   cycle equation, no vanishing tail) are all consequences of `spec M`. What
   a hit-forcing argument needs is an *upper* bound or a recurrence statement
   in the contracting space: the offsets of `S` are finitely many purely
   periodic `β`-expansions in the digit set `F` (Lemma 5.45), and the hitting
   condition asks whether the contracting projection of the prefix set of
   `σ^m(i)` minus that of `σ^m(j)` (which accumulates on Rauzy-fractal
   pieces) meets the projected offset. That is the (F)-property /
   super-coincidence geometry of Ito–Rao and, for `|det M|>1`,
   Minervino–Thuswaldner. None of the eleven notes engages it; the literature
   gate of 09-15 named it and set it aside. The firewall against a Euclidean
   internal space in the non-unimodular case is right, and it is also why the
   zipper branch cannot be closed by more bookkeeping on `Z^d`.

5. **The aligned branch is a cleaner, separately meaningful target.** Strong
   coincidence for ternary PIP substitutions is a named open problem. The BD
   eliminator, the hub-star normal form, and the coaccessible-core bound are
   genuinely about it and do not depend on the zipper machinery. Progress
   there is publishable on its own even if `OP` is not closed.

6. **Two dormant tracks are correctly parked.** G1b-2 (equivalent to G1) and
   the word-level sieves of programme B do not feed the headline. The
   manuscript's own discussion (l.1026) locates G1b-2 in an internal space
   with a non-Archimedean factor; nobody is working there.

---

## E. Status-surface consistency

Ten surfaces were cross-checked (README, architecture 09-14, weekly ledger
09-14, claim map, conjecture ledger, proof ladder, `Ledger.tla` and
`ledger-index`, verification architecture, manuscript, `claim_governance`).
They agree on the status of every headline claim: G1 open, G1b-1 proved,
G1b-2 open, Thm 4.22 proved, 5.35 open, 5.36 proved, 5.37 imported, 5.38
conditional, C1 open, concentration and wedge productivity open, realization
an open bridge, PDS open. The disagreements are in wording and bookkeeping,
ranked by consequence:

1. **Seed legality.** `README.md:88` and `completion-ledger-2026-09-14.md:63,247`
   say Theorem 5.38 needs one *legal* swap seed. The manuscript (l.778–784),
   the architecture doc (l.18) and the hypothesis firewall on every surface
   say legality is **not** assumed and must not be added. The primary status
   surface contradicts the theorem it summarizes.
2. **What "Open Problem 5.35" names.** In the manuscript (l.761, 1012) it is
   the all-vertex statement; README, claim map (l.55), `ledger-index` (l.43)
   and issue #84 attach the number to the weaker one-seed statement that
   Theorem 5.38 actually needs. Either renumber or add the one-seed form as
   its own labelled problem.
3. **Stale route paragraph.** `verification-architecture.md:85–90` still
   calls `C4 ⟹ C3-local ⟹ C2 ⟹ SCCProducer, G1 + SCCProducer ⟹ PDS` "the
   current route"; `tla/check.sh:25–28` likewise omits the overlap route.
4. **`ProvedDef` semantics are invisible to a reader.** `G1`, `SCCProducer`,
   `PDS`, `PDSOverlapRoute`, `LoadBearingSCC` sit in `ProvedDef`
   (`Ledger.tla:152,180,170`) because the set means "record accepted given
   its prerequisites"; only `make_ledger.py` comments and three `status_note`
   fields say so. Seven `ProvedDef` nodes cite no locatable statement
   (`LocalWitnessInjectivity`, `MassBalanceK2Obstruction`,
   `AlgebraicEmbedding`, `WedgeBound`, `InterBlockCancellation`,
   `DominantK2Source`, `LoadBearingSCC`: archived v15/v34 results absent from
   the manuscript), and UD is cited as "Theorem 3.1" (claim map l.30,
   `make_ledger.py:47`) where the manuscript has Theorem 2.8 and 3.1 is the
   conditional main theorem.
5. **Theorem 5.38's tag.** The tex (l.784) tags it `\Status{Theorem}` with a
   parenthetical; the vocabulary (l.80) defines `\Status{Conditional}` for
   exactly this case and uses it at l.212. Every repo surface says
   "conditional theorem".
6. **Four status-changing PRs after the dated surfaces.** The affine cycle
   identity (#91), periodic-patch collar (#101), unimodular no-collapse
   (#123), seed-growth lemma (#115), the conditional coincidence-level bound
   (#127) and the prefix-translation normal form (#133) are recorded as
   proved in the claim map and `claim_governance` but appear in none of
   `Ledger.tla`, the architecture doc, or the manuscript. The weekly ledger
   header says "through PR #82", the claim map "through PR #86"; both bodies
   cite later PRs.
7. **Missing ledger edge.** Thm 5.32(iv) proves `OP_all ⟹ SCCProducer`;
   `Ledger.tla` has no such edge, so the model cannot derive C1 from the
   overlap gate.
8. Minor: `DominantCubicCapture` is "historical restricted" in the claim map
   and `ProvedDef` in the ledger; `claim_governance` carries both; priority
   numbering of wedge productivity and realization differs between the
   weekly ledger and the two prose ledgers; two surfaces cite a "manuscript
   status table" that does not exist.

**TLA models.** Twelve models, all in `check.sh`. The overlap route is
covered in both directions: `MCLedgerOverlapGateAssumed` reaches
`PDSOverlapRoute` and asserts `PDS`, `G1`, `SCCProducer` not established;
`MCLedgerOpen` and `MCLedgerImports` assert `PDSOverlapRoute` not
established. All eight ledger closures were recomputed independently in
Python and match their `Reachable` sets. TLC itself was not run here.

**09-15 engineering audit follow-up.** Not acted on: no LICENSE or
CITATION file; unpinned toolchain fetches in `ci.yml` (pixi, latest
tla2tools, elan master); no lint/type gate; `verify_all.sh` still swallows
pytest output. Partial: 59 of 84 `docs/*.md` remain unindexed, including
the audits. Done: Mojo test runner, NLAP prefixes.

---

## F. Recommendations, ranked

1. **Verify Imported Theorem 5.37 against Barge–Štimac–Williams §3 by a human
   reader**, on the three points in B.1, and record the check in
   `claim-status-and-source-map`. State Thm 5.38's hypothesis in the
   full-measure form as well as the dense form.
2. **Reword the headline** on README, architecture and ledger surfaces: "the
   one open premise contains two-sided strong coincidence and is equivalent
   to PDS for unimodular `σ` (BK converse, not imported); Theorem 5.38 removes
   the finiteness hypothesis, it does not reduce the conjecture." Record the
   BK converse as a cited, unused statement so readers can see it.
3. **Stop adding necessary-condition layers to the zipper branch** unless a
   layer comes with a completeness or uniformity statement. Redirect the
   zipper work to the contracting-space hitting problem explicitly: import
   (as imported, with hypotheses) what Ito–Rao / Barge–Kwapisz / Minervino–
   Thuswaldner prove about it, and state the exact residual obligation in
   the non-unimodular case.
4. **Split issue #84** into the aligned branch (strong coincidence for
   ternary PIP; BD eliminator, hub-star, coaccessible bound) and the zipper
   branch (super-coincidence hitting), each with its own literature baseline.
5. Make the manuscript-integrity tests skip with a reason when `pypdf` is
   unavailable instead of failing 38 tests; CI masks this today.
6. Leave programme B and G1b-2 parked; do not reopen fixed-size sieves.
