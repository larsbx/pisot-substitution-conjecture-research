# Audit — 2026-09-10: repository state and mathematical progress

**Status:** amended after independent review of PR #26 and automated review of this amendment. This version preserves the mathematical audit while correcting the roadmap and two hypothesis/bridge details.

## 1. Headline

The mathematics added since 2026-09-08 is internally coherent as far as this audit can verify and is generally labelled with appropriate guardrails. The main weakness is no longer an obvious false lemma; it is **program shape**.

The repository accumulated a long chain of increasingly fine necessary-condition sieves for a hypothetical finite closed nonproductive SCC. Those reductions are useful and mostly uniform once such an SCC is given, but the recent `|C|=3` layers are calibration rather than a plausible complete proof strategy. PRs #24 and #25 have now been treated exactly that way and the fixed-size sieve track is closed as a main direction.

The next main theorem target should be **uniform in `|C|`** and use information the existing algebraic sieves do not: the prefix-difference return structure and recognizability/supertile alignment.

The second weakness is record drift. A follow-up record-sync PR now updates `README.md`, `tla/Ledger.tla`, `docs/conjecture-ledger.md`, `docs/proof-ladder.md`, and `docs/verification-architecture.md` to the live C4 state.

## 2. Scope and notation

`FCS` means that a finite closed nonproductive recurrent noncoincident SCC `C` of the balanced-pair graph is already given.

This distinction matters:

- **G1 is used to pass from a global nonproductive region to such a finite sink SCC.**
- Once an FCS is given, many subsequent structural lemmas do not separately assume global BPA finiteness.
- The global endpoint synchronization theorem does not require Pisot or G1 at all.

So it is too broad to say that all recent work is "inside the G1 box." The accurate statement is:

> The current proof route from failure of C1 to a finite obstruction uses G1 at the sink-SCC extraction step; most C4 structure is then conditional on the resulting finite closed SCC rather than on global BPA finiteness itself.

## 3. Record drift found by the audit

The following drift was confirmed:

1. `tla/Ledger.tla` predated the C4 reduction stack and lacked nodes for sink-SCC reduction, C3 locality, endpoint quotients, Parikh intertwining, orientation monodromy/spectrum, signed defect intertwiners, W3 low-growth, degree-4, the mod-3 sieve, parity, and ordered mid-area factorization.
2. `README.md` still described the boundary-synchronization stage as if the subsequent C4 work had not happened.
3. `docs/conjecture-ledger.md` still treated C3/C4 as earlier-stage targets and omitted the proved rungs and retired endpoint-cone route.
4. `docs/proof-ladder.md` foregrounded the old v34 lower bound `rho(N_C) >= beta |beta_2|`; for a strict closed nonproductive component the Parikh intertwiner now gives the exact equality `rho(N_C)=beta`.
5. `docs/verification-architecture.md` lacked discrepancy D4: the archived unsigned degree-3 SCC interface `L_C N_C = Phi_3 L_C` omits child-orientation signs. The correct normalized relation is `Q_3 S = Phi_3 Q_3`, with `S=A-B`.
6. Issue #2 was mathematically superseded in part by the exact Mojo PIP screen but its reproducibility contract was not complete. It has therefore been re-scoped rather than closed.

## 4. Audited reduction stack

The following results remain sound under their stated assumptions.

| Result | Actual hypothesis | Audit verdict |
|---|---|---|
| Sink-SCC reduction | G1 plus existence of a nonproductive state | correct; finite graph argument |
| Newborn locality / C3-local reduction | FCS | correct |
| 27 endpoint maps -> 7 types; synchronization quotient permutation | finite 3-letter endpoint maps | correct; exhaustive |
| Globally synchronizing `sigma_+` or `sigma_-` implies productivity | none | correct and unconditional |
| 1/3/9 endpoint-signature bound | FCS | correct |
| `P_C N_C = M P_C`, rank 3, `|C|>=3`, `chi_M | chi_N`, `rho(N_C)=beta` | FCS + irreducible `chi_M` | correct |
| Z/2 orientation cocycle and oriented double cover | FCS | correct |
| Even/odd incidence split `N=A+B`, `S=A-B`, `rho(S)<=beta` | FCS + Parikh/Perron equality when the bound is stated as `<=beta` | correct |
| Signed first-defect intertwiner `Q_r S = Phi_r Q_r` | FCS | correct in the documented first-defect regime; general free-Lie membership should still be formalized cleanly |
| Degree 2 full exterior-square quotient | FCS + nonzero `K2` + irreducible cubic `chi_M` | correct; irreducibility is needed to force `rank(Q2)=3` |
| Degree 3 low-growth image is determinant/centralizer-valued | FCS + `K2=0` + standing irreducibility/semisimplicity bookkeeping | correct |
| Degree 4 floor | FCS + lower defects zero | correct |
| Arbitrary-degree mod-3 multidegree sieve | FCS + standing PIP cubic regime | correct |
| Mod-2 parity size sieve | FCS + degree 2 + full exterior-square quotient | correct; independently recomputed |
| Three-state trace conditions | FCS + `|C|=3` | correct but weak as an eliminator |
| Ordered mid-area identity | actual ordered zero-return factorization | correct; first current layer that retains child order |
| Mean-area Sylvester lift | FCS + `|C|=3` + rank-three Parikh intertwiner | correct calibration |
| Cramer integral-image certificate | same as previous | correct and complete for one nonsingular RHS test |

What remains unproved is unchanged in substance: **G1, C1, C2, C3, C4, and PSC/PDS in the general target regime.** None of the necessary-condition layers has yet been shown jointly inconsistent for every possible counterexample SCC.

## 5. Current conditional proof architecture

The machine-checked bridge remains

```text
G1 and C1 => PDS
```

in the standing alphabet-3 PIP regime.

Under G1, the current **sufficiency** chain is

```text
C4 => C3-local => C2 => C1.
```

These arrows are proof-dependency/sufficiency arrows. They must **not** be encoded or described as converses. In particular, a dependency ledger may say that the present proof of C1 passes through C4; it must not assert `C1 => C4` or an equivalence.

A hypothetical FCS witnessing failure of the current C4 target must simultaneously satisfy endpoint nonsynchronization, the Parikh rank/Perron constraints, the orientation/sign constraints, the first-defect representation constraints, and the ordered zero-return factorization identities.

Exact finite calibration over the established 4,554 alphabet-3 PIP substitutions remains evidence only: all BPA builds terminate below the cap, every observed sink is productive, 385,902 reachable noncoincident states have first defect degree 2, 24 have degree 3, and none has degree >=4.

## 6. G1 and the balanced-pair literature: corrected bridge obligation

Akiyama, Barge, Berthé, Lee, and Siegel, *On the Pisot Substitution Conjecture* (2015), Theorem 5.3, states for an irreducible Pisot substitution that pure discrete symbolic spectrum is equivalent to termination with coincidence of the standard balanced-pair algorithm; it also gives a swap-seed criterion of the form `(ij,ji)`. The survey points to earlier balanced-pair work including Sirvent–Solomyak and Martensen.

This is highly relevant to G1, but it does **not** justify skipping from one convenient seed to the repository's all-seed graph.

The repository uses the finite family of legal swap seeds

```text
(ab,ba),  a != b,
```

modulo side-swap normalization. Its graph is the union of the single-seed irreducible-factor closures.

Therefore the repository bridge needed for a bare theorem

```text
PDS => G1
```

must establish one of the following:

1. PDS implies termination/finiteness for **every legal swap-seed closure** represented in the repository graph; or
2. a separate theorem reduces the full legal swap-seed family to one seed and proves that every other seed closure is contained in or reachable from that terminating closure.

Termination from just one seed, without such a family-reduction theorem, is not enough to prove finiteness of the all-seed union.

`docs/bpa-literature-bridge.md` records the repository union lemma and keeps the remaining seed-family implication explicit. Until it is closed, the safe statement is:

> Literature gives a PDS/termination criterion for the standard balanced-pair algorithm; applicability to the repository's entire normalized legal swap-seed union still requires the seed-family bridge.

## 7. Sieve saturation and strategic pivot

The recent three-state work is useful calibration: it shows exactly which invariants distinguish increasingly realistic golden negatives. It should now stop as a main proof spine.

For a fixed finite BPA, counterexample existence is decidable by building the graph. A new finite sieve has proof value only when accompanied by a **uniform completeness statement** over substitutions or SCC sizes.

The two strongest theorem-grade ingredients not yet applied to the actual factorization obstruction are:

### 7.1 Prefix-difference return structure plus Pisot contraction

For a balanced pair `T=(u,v)`, define

```text
D_T(k)=Parikh(u[:k])-Parikh(v[:k]).
```

Irreducibility means `D_T(k)!=0` at every interior cut. Under substitution, image ordering expands this into a piecewise lattice walk; its returns to zero are exactly the balanced child boundaries.

For a finite closed SCC, state lengths are bounded while iterates grow at rate `beta`, so the inflated pair contains bounded-gap zero returns. The next uniform theorem should couple that return density to contraction in the stable Pisot directions.

### 7.2 Recognizability / supertile-boundary alignment

In the gauge-trivial orientation case, the SCC gives two distinct Parikh-equal word morphisms with a common derived substitution. Existing notes do not prove that balanced-child cuts align with `sigma`-supertile boundaries. Mossé-style recognizability is the natural tool for upgrading recurrent cut patterns into alignment or contradiction.

The next proof target should therefore be an **alignment-or-return-density lemma**, not another `|C|=3` arithmetic sieve.

## 8. Revised action list

1. **Repair the canonical record.** Extend `tla/Ledger.tla` and synchronize README, conjecture ledger, proof ladder, verification architecture, and issue #9. Encode `C4 => C3-local => C2 => C1` only as current sufficiency/dependency arrows. **In progress in the follow-up record-sync PR.**
2. **Write the BPA definition/seed-family bridge.** Prove the repository all-seed union lemma and isolate the remaining literature implication across every legal swap seed. **In progress in `docs/bpa-literature-bridge.md`.**
3. **Settle PRs #24/#25 as calibration and stop extending the fixed-size track.** **Completed.**
4. **Re-scope issue #2** to deterministic exact corpus screening/export. **Completed.**
5. **Start the uniform C4 theorem branch.** Formalize the prefix-difference inflation law, zero-return/block-boundary correspondence, and recognizability alignment target. Search first for false lemmas/counterexamples before investing in a proof.
6. **Issue hygiene.** Re-scope issue #1 to the current sink/locality manuscript formulation and retain #3 as the live proof-record consistency audit. **Completed.**

## 9. Bottom line

The C4 reduction stack is materially stronger than the old canonical record, but the three-state sieve direction has reached diminishing returns. The credible path is now two-pronged:

- settle how standard balanced-pair termination controls the repository's **full legal swap-seed family**;
- attack C4 uniformly through prefix-difference returns and recognizability.

That is the roadmap this amended audit recommends.